# Акселератор МОСОБАВУЗ

Корпоративный акселератор инноваций - современный лендинг для платформы МОСОБАВУЗ, созданный на основе дизайна Figma.

![Preview](https://3000-ig729j04ac5i54v3vgzmx-33d3e90c.manusvm.computer)

## 📋 Описание проекта

Это одностраничный веб-сайт (landing page) для акселератора корпоративных инноваций МОСОБАВУЗ. Проект создан для привлечения участников, предпринимателей и учебных заведений к программе акселерации.

### Основные секции:

- **Главная секция (Hero)** - с градиентным фоном и ключевыми показателями (40+ проектов, 15+ университетов)
- **Экосистема успеха** - 4 колонки с описанием преимуществ (Партнерство, Сообщество, Продвижение, Финансирование)
- **Как принять участие?** - 2 карточки с призывом к действию
- **Наши партнеры** - логотипы партнерских организаций
- **Финальный CTA** - призыв стать участником
- **Футер** - навигация и контактная информация

## 🛠 Технологический стек

- **React 19** - современная библиотека для создания пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript для надежности кода
- **Vite** - быстрый сборщик и dev-сервер
- **Tailwind CSS 4** - utility-first CSS фреймворк
- **Wouter** - легковесный роутинг для React
- **shadcn/ui** - переиспользуемые UI компоненты
- **Roboto** - основной шрифт (Google Fonts)

## 📦 Структура проекта

```
accelerator-mosobavuz/
├── client/                      # Frontend приложение
│   ├── public/                  # Статические файлы
│   │   └── assets/              # Изображения и иконки из Figma
│   ├── src/
│   │   ├── components/          # React компоненты
│   │   │   └── ui/              # shadcn/ui компоненты
│   │   ├── pages/               # Страницы приложения
│   │   │   ├── Home.tsx         # Главная страница
│   │   │   └── NotFound.tsx     # 404 страница
│   │   ├── contexts/            # React контексты
│   │   ├── hooks/               # Пользовательские хуки
│   │   ├── lib/                 # Утилиты
│   │   ├── App.tsx              # Корневой компонент с роутингом
│   │   ├── main.tsx             # Точка входа React
│   │   └── index.css            # Глобальные стили и Tailwind
│   ├── index.html               # HTML шаблон
│   ├── package.json             # Зависимости проекта
│   ├── tsconfig.json            # Конфигурация TypeScript
│   ├── tailwind.config.ts       # Конфигурация Tailwind
│   └── vite.config.ts           # Конфигурация Vite
├── todo.md                      # Список задач проекта
└── README.md                    # Этот файл
```

## 🚀 Быстрый старт

### Предварительные требования

Убедитесь, что у вас установлены:

- **Node.js** версии 18.0 или выше ([скачать](https://nodejs.org/))
- **pnpm** (рекомендуется) или npm

Проверить версии:

```bash
node --version   # должно быть >= 18.0
pnpm --version   # должно быть >= 8.0
```

### Установка pnpm (если не установлен)

```bash
npm install -g pnpm
```

## 📥 Установка и запуск

### 1. Клонирование репозитория

```bash
# Если проект в Git
git clone <repository-url>
cd accelerator-mosobavuz

# Или просто перейдите в директорию проекта
cd accelerator-mosobavuz
```

### 2. Установка зависимостей

```bash
# Перейдите в директорию client
cd client

# Установите все зависимости
pnpm install

# Альтернативно, если используете npm:
# npm install
```

Это установит все необходимые пакеты, указанные в `package.json`.

### 3. Запуск в режиме разработки

```bash
# Запустите dev-сервер
pnpm dev

# Альтернативно:
# npm run dev
```

После запуска вы увидите сообщение:

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: http://192.168.x.x:3000/
```

Откройте браузер и перейдите по адресу: **http://localhost:3000**

### 4. Горячая перезагрузка (Hot Module Replacement)

При изменении файлов в директории `src/`, браузер автоматически обновится без полной перезагрузки страницы. Это значительно ускоряет разработку.

## 🏗 Сборка для продакшена

### Создание production сборки

```bash
cd client

# Сборка проекта
pnpm build

# Альтернативно:
# npm run build
```

Это создаст оптимизированную сборку в директории `client/dist/`:

```
client/dist/
├── assets/           # Минифицированные JS, CSS и изображения
├── index.html        # Главный HTML файл
└── ...
```

### Предварительный просмотр production сборки

```bash
# Запустите локальный сервер для просмотра
pnpm preview

# Альтернативно:
# npm run preview
```

Откройте: **http://localhost:4173**

## 🌐 Деплой

### Вариант 1: Статический хостинг

Содержимое папки `client/dist/` можно разместить на любом статическом хостинге:

- **Vercel**: `vercel --prod`
- **Netlify**: перетащите папку `dist` в Netlify Drop
- **GitHub Pages**: используйте GitHub Actions
- **Cloudflare Pages**: подключите репозиторий

### Вариант 2: Через Manus Platform

Проект уже настроен для деплоя через Manus:

1. Сохраните checkpoint через интерфейс Manus
2. Нажмите кнопку **"Publish"** в Management UI
3. Ваш сайт будет доступен по адресу `*.manus.space`

## 🎨 Кастомизация

### Изменение цветов

Отредактируйте файл `client/src/index.css`:

```css
:root {
  --primary: var(--color-blue-700);
  --background: oklch(1 0 0);
  /* ... другие цвета */
}
```

### Изменение шрифта

1. Добавьте новый шрифт в `client/index.html`:

```html
<link href="https://fonts.googleapis.com/css2?family=YourFont:wght@400;700&display=swap" rel="stylesheet" />
```

2. Обновите `client/src/index.css`:

```css
body {
  font-family: 'YourFont', sans-serif;
}
```

### Добавление новых страниц

1. Создайте файл в `client/src/pages/NewPage.tsx`
2. Добавьте роут в `client/src/App.tsx`:

```tsx
<Route path="/new-page" component={NewPage} />
```

### Изменение контента

Весь контент находится в файле `client/src/pages/Home.tsx`. Просто отредактируйте текст, изображения или структуру компонентов.

## 🖼 Работа с изображениями

### Добавление новых изображений

1. Поместите изображения в `client/public/assets/`
2. Используйте в коде:

```tsx
<img src="/assets/your-image.webp" alt="Description" />
```

### Оптимизация изображений

Рекомендуется использовать:
- **WebP** формат для фотографий
- **SVG** для иконок и логотипов
- Сжатие через [TinyPNG](https://tinypng.com/) или [Squoosh](https://squoosh.app/)

## 🧪 Разработка

### Доступные команды

```bash
# Разработка
pnpm dev              # Запуск dev-сервера
pnpm build            # Production сборка
pnpm preview          # Просмотр production сборки
pnpm lint             # Проверка кода линтером
pnpm type-check       # Проверка типов TypeScript
```

### Добавление новых зависимостей

```bash
# Установка новой библиотеки
pnpm add library-name

# Установка dev-зависимости
pnpm add -D dev-library-name
```

### Использование shadcn/ui компонентов

Проект уже включает shadcn/ui. Для добавления новых компонентов:

```bash
# Пример: добавление компонента Dialog
pnpm dlx shadcn-ui@latest add dialog
```

## 🐛 Решение проблем

### Порт 3000 уже занят

```bash
# Используйте другой порт
pnpm dev --port 3001
```

### Ошибки при установке зависимостей

```bash
# Очистите кеш и переустановите
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### Изображения не загружаются

Убедитесь, что:
1. Файлы находятся в `client/public/assets/`
2. Путь начинается с `/assets/` (без `public`)
3. Имена файлов совпадают (учитывается регистр)

### Проблемы с TypeScript

```bash
# Проверьте ошибки типов
pnpm type-check

# Перезапустите TypeScript сервер в VS Code
# Ctrl+Shift+P -> "TypeScript: Restart TS Server"
```

## 📱 Адаптивность

Сайт полностью адаптивен и корректно отображается на:

- 📱 Мобильных устройствах (320px+)
- 📱 Планшетах (768px+)
- 💻 Десктопах (1024px+)
- 🖥 Больших экранах (1280px+)

Используются Tailwind breakpoints:
- `sm:` - от 640px
- `md:` - от 768px
- `lg:` - от 1024px
- `xl:` - от 1280px

## 🔒 Безопасность

- Нет серверной части - только статический frontend
- Все зависимости регулярно обновляются
- Используются официальные CDN для шрифтов

## 📄 Лицензия

Этот проект создан для МОСОБАВУЗ. Все права защищены.

## 👥 Контакты и поддержка

Для вопросов и предложений:
- Официальный сайт: [manus.im](https://manus.im)
- Документация: [docs.manus.im](https://docs.manus.im)

## 🙏 Благодарности

- Дизайн создан на основе Figma макета
- Использованы компоненты [shadcn/ui](https://ui.shadcn.com/)
- Иконки и изображения предоставлены заказчиком

---

**Создано с помощью [Manus AI](https://manus.im)** 🚀

