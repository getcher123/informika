# 🛠 Как мы вносим изменения

## 📌 Ветвление

- Основная ветка: `main` (защищена).
- Рабочие ветки: `feat/...`, `fix/...`, `chore/...`, `docs/...`, `refactor/...`.

## 📝 Коммиты

- Используем Conventional Commits:
  - `feat:` новая фича
  - `fix:` исправление
  - `docs:`, `refactor:`, `chore:` — по назначению

## ✅ Перед Pull Request

1. Запусти проверки:
   ```bash
   npm run fmt:write
   npm run lint:js && npm run lint:css
   composer cs:write
   composer stan
   ```
